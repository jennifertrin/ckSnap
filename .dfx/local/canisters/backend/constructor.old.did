type Proposal = record {
  title : text;
  description : text;
  contract_address : text;
  amount : float64;
};
service : {
  get : (text) -> (Proposal) query;
  get_all_proposals : () -> (vec Proposal) query;
  update : (Proposal) -> ();
}
